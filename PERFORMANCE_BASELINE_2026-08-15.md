# Performance baseline — 2026-08-15

## Условия измерения

- Production-сборка: `npm run build` и локальный `vite preview`.
- Lighthouse 13.4.1.
- Mobile: стандартная эмуляция Lighthouse (Moto G Power 2022 и throttling).
- Desktop: стандартный desktop preset Lighthouse.
- Каждый маршрут измерен отдельным чистым запуском Chrome.
- Лабораторный TBT используется как диагностический показатель нагрузки. Полевой INP можно получить только после накопления реальных пользовательских данных.

## Mobile

| Страница | Performance | FCP | LCP | TBT | CLS | Передано | JS | Изображения |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Главная | 54 | 3,89 с | 21,16 с | 313 мс | 0 | 3,71 МБ | 611 КБ | 3,04 МБ |
| Каталог | 55 | 3,81 с | 8,71 с | 369 мс | 0 | 1,34 МБ | 609 КБ | 660 КБ |
| Категория «Маркетплейс» | 64 | 3,65 с | 8,56 с | 171 мс | 0 | 1,32 МБ | 609 КБ | 660 КБ |
| Автоответы на отзывы | 64 | 3,65 с | 14,41 с | 172 мс | 0 | 2,52 МБ | 610 КБ | 1,86 МБ |
| Карточка с изображением 3,8 МБ | 64 | 3,65 с | 27,76 с | 173 мс | 0 | 5,23 МБ | 609 КБ | 4,58 МБ |

## Desktop

| Страница | Performance | FCP | LCP | TBT | CLS | Передано |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Главная | 80 | 0,77 с | 3,57 с | 0 мс | 0 | 3,70 МБ |
| Каталог | 94 | 0,77 с | 1,61 с | 0 мс | 0 | 1,34 МБ |
| Категория «Маркетплейс» | 96 | 0,65 с | 1,37 с | 0 мс | 0 | 1,31 МБ |
| Автоответы на отзывы | 87 | 0,65 с | 2,41 с | 0 мс | 0 | 2,52 МБ |
| Карточка с изображением 3,8 МБ | 78 | 0,65 с | 4,29 с | 0 мс | 0 | 5,23 МБ |

## Основные причины замедления

### 1. Изображения

- `icon.png`: 660 КБ и 1254×1254 px, хотя в шапке отображается как 32×32 px.
- `automation-enterprise.png`: 1,22 МБ, на mobile отображается примерно как 344×229 px.
- `1c-ozon-sync-case.png`: 1,16 МБ, на mobile отображается примерно как 304×218 px.
- `marketplace-auto-review-replies.png`: 1,20 МБ.
- `1c-dialog-ai-agent.png`: 3,92 МБ.
- На главной Lighthouse оценивает потенциальную экономию от оптимизации изображений примерно в 2,94 МБ.

### 2. JavaScript

- Общий локальный бандл `prerender-*.js`: примерно 329 КБ переданных данных.
- Google Analytics: примерно 172 КБ.
- Яндекс Метрика: примерно 95–112 КБ.
- Общий объём скриптов первого захода: около 609–611 КБ.
- На главной mobile основной поток занят примерно 7,4 с, из них выполнение JavaScript — около 4,7 с.
- Локальный общий бандл занимает около 4,55 с boot-up времени в mobile-эмуляции.
- Lighthouse оценивает неиспользуемый JavaScript примерно в 213 КБ на главной.

### 3. Шрифты

- CSS обращается к Google Fonts за пятью семействами.
- В тесте запрос Google Fonts вошёл в критическую цепочку, но файлы шрифтов не были получены. Это делает текущий baseline скорее оптимистичным по переданным байтам шрифтов.

### 4. LCP-кандидаты

- Главная: H1 «AI агенты и автоматизации для бизнеса» — 21,16 с на mobile.
- Каталог: H1 «ИИ-решения для бизнеса» — 8,71 с.
- В карточках LCP-кандидатом становится крупный текстовый блок, а итоговый момент LCP сильно отодвигается загрузкой страницы и анимациями.

## Что уже хорошо

- CLS равен 0 на всех десяти прогонах.
- TTFB локального production-preview около 4–10 мс и не является причиной замедления.
- Desktop-каталог и категория уже показывают 94–96 баллов.
- CSS весит около 15 КБ gzip и не является главным приоритетом.

## Приоритет следующего этапа

1. Сделать компактный логотип и внедрить WebP/AVIF с responsive-размерами.
2. Добавить lazy loading всем изображениям ниже первого экрана.
3. Разобрать общий JS-бандл и отделить тяжёлые блоки главной.
4. Убрать лишние семейства Google Fonts и перейти на локальные WOFF2.
5. Повторить те же десять замеров после каждой крупной группы изменений.

## Контрольный замер после оптимизации логотипа

Каталог, mobile Lighthouse:

| Показатель | До | После |
| --- | ---: | ---: |
| Performance | 55 | 66 |
| FCP | 3,81 с | 3,71 с |
| LCP | 8,71 с | 5,64 с |
| TBT | 369 мс | 184 мс |
| Всего передано | 1,34 МБ | 683 КБ |
| Изображения | 660 КБ | 1,7 КБ |

Логотип интерфейса уменьшен с 660 КБ до 0,7–1,2 КБ. В шапке браузер выбирает вариант 64×64 px для экрана с высокой плотностью и отображает его в размере 32×32 px.

## Контрольный замер после полного этапа оптимизации изображений

Mobile Lighthouse, итоговый прогон:

| Страница | LCP до | LCP после | Передано до | Передано после | Изображения до | Изображения после |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Главная | 21,16 с | 5,37 с | 3,71 МБ | 670 КБ | 3,04 МБ | 1,7 КБ |
| Каталог | 8,71 с | 5,45 с | 1,34 МБ | 687 КБ | 660 КБ | 1,7 КБ |
| Категория «Маркетплейс» | 8,56 с | 5,62 с | 1,32 МБ | 670 КБ | 660 КБ | 1,7 КБ |
| Автоответы на отзывы | 14,41 с | 5,23 с | 2,52 МБ | 683 КБ | 1,86 МБ | 20 КБ |
| Карточка с изображением 3,8 МБ | 27,76 с | 5,53 с | 5,23 МБ | 683 КБ | 4,58 МБ | 19 КБ |

Lighthouse Performance после оптимизации составил 56–71 на mobile. Итоговый балл и TBT заметно колеблются между одиночными лабораторными прогонами из-за общего JavaScript и сторонней аналитики; объём переданных данных и сокращение LCP воспроизводятся стабильнее.

Конвейер создаёт AVIF и WebP в ширинах 480, 768, 1200 и 1600 px без увеличения маленьких исходников. Компонент выбирает вариант через `picture`, `srcset` и `sizes`, добавляет собственные размеры, `loading="lazy"` и `decoding="async"`.

## Контрольный замер после разделения JavaScript

- Начальный локальный JS уменьшен с 335 КБ до 105 КБ gzip.
- Главная, каталог, карточка, контакты и правовые страницы загружаются отдельными чанками.
- `framer-motion` вынесен из общего shell в отдельный чанк около 38 КБ gzip.
- Полные данные каталога больше не импортируются через Footer.
- Google Analytics и Яндекс Метрика загружаются после `load`/idle или первого взаимодействия; команды первого просмотра и SPA-переходов ставятся в очередь заранее.

Mobile Lighthouse, один итоговый прогон после JavaScript-разделения:

| Страница | Performance до | Performance после | FCP до | FCP после | LCP до | LCP после | JS до | JS после |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Главная | 56 | 74 | 3,15 с | 2,81 с | 5,37 с | 3,26 с | 616 КБ | 445 КБ |
| Каталог | 71 | 78 | 3,75 с | 2,66 с | 5,45 с | 3,51 с | 616 КБ | 534 КБ |
| Категория «Маркетплейс» | 66 | 89 | 3,82 с | 2,08 с | 5,62 с | 3,40 с | 618 КБ | 534 КБ |
| Автоответы на отзывы | 71 | 85 | 3,65 с | 2,36 с | 5,23 с | 3,59 с | 616 КБ | 588 КБ |
| Карточка с тяжёлым исходником | 70 | 70 | 3,66 с | 3,03 с | 5,53 с | 5,62 с | 617 КБ | 588 КБ |

Объём JS в Lighthouse включает аналитические библиотеки, которые успевают загрузиться ближе к концу прогона, но больше не находятся в критической цепочке первого отображения. Одиночные значения TBT и итогового Performance колеблются; устойчивые изменения — уменьшение начального чанка, более ранний FCP и раздельная загрузка маршрутов.

## Этап 5. Упрощение анимаций и рендеринга

- Удалены все 45 компонентов `motion` и 17 отдельных `whileInView`-анимаций.
- `framer-motion` полностью удалён из зависимостей; отдельный чанк около 38 КБ gzip больше не загружается ни на одном маршруте.
- Простые появления и hover-эффекты переведены на CSS с общей поддержкой `prefers-reduced-motion`.
- Карточки каталога и блоки Markdown больше не создают отдельный `IntersectionObserver` для каждого элемента.
- Для секций ниже первого экрана добавлены `content-visibility: auto` и резервная внутренняя высота, чтобы браузер откладывал layout/paint длинных страниц.
- Canvas-глобус работает с ограничением 30 FPS, меньшим числом частиц на мобильных и останавливается вне экрана или в фоновой вкладке. При reduced motion рисуется один статичный кадр.
- Исправлено несовпадение SSR и клиентского дерева: контейнер уведомлений теперь присутствует и на сервере. Production-гидратация проходит без повторного выбрасывания серверного DOM и без ошибок React 418/423.

Контрольная production-проверка: главная, каталог и карточка услуги отображаются без ошибок консоли; H1, Canvas, карточки и длинный контент присутствуют. Сборка, ESLint, TypeScript-проверка и SEO-пререндер 72 карточек проходят успешно.

## Оптимизация deployment-артефакта

- Логический размер `dist` уменьшен с 131,3 МБ до 24,7 МБ.
- После генерации AVIF/WebP из deployment-артефакта удаляются 77 исходных изображений общим объёмом 100,5 МБ.
- Исходники остаются в `public` и репозитории: очистка затрагивает только заново создаваемую папку `dist`.
- Рекламные изображения из `images/direct-review-replies`, служебные `.DS_Store` и `.pipeline-state.json` исключаются только из deployment-артефакта.
- Перед удалением скрипт требует наличия обоих форматов — AVIF и WebP. После очистки отдельная проверка контролирует ссылки из HTML/CSS/manifest и все варианты из сгенерированного реестра.
- Проверено 598 ссылок собранных страниц и 614 оптимизированных вариантов. SEO-пререндер 72 карточек проходит после очистки.
- FTP mirror запускается с четырьмя параллельными передачами и возможностью продолжения; аварийный лимит шага увеличен с 10 до 20 минут.
